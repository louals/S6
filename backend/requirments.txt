# --- Core FastAPI stack ---
fastapi
uvicorn[standard]

# --- PostgreSQL with SQLAlchemy async ---
sqlalchemy>=2.0
asyncpg
greenlet

# --- MongoDB async ---
motor

# --- Pydantic for validation ---
pydantic>=2.0
pydantic-settings

# --- Env file loader ---
python-dotenv

# --- UUID, datetime, etc. are built-in ---

# --- Password hashing ---
passlib[bcrypt]

# --- JWT for auth (if used) ---
python-jose[cryptography]

# --- For running tests (if needed) ---
pytest
httpx
